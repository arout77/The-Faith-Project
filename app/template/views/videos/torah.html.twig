{% extends 'porto.html.twig' %}
{% block title %}Video Categories{% endblock %}

{% block meta_description %}
<meta name="description" content="Videos of The Bible Series. In this series, we dive into the two-part work of Luke and Acts and gain a unique perspective on the life of Jesus and the early Church." />
{% endblock %}

{% block keywords %}
<meta name="keywords" content="Animated videos, videos about the Bible, Bible web series" />
{% endblock %}

{% block content %}
<div class="container mb-5">
    <div class="row">
        <div class="col-lg-3 position-relative">
            <div class="pin-wrapper" style="height: 241.391px;">
                <aside class="sidebar" id="sidebar" data-plugin-sticky="" data-plugin-options="{'minWidth': 991, 'containerSelector': '.container', 'padding': {'top': 110}}" style="width: 261px;">
                    <h4 class="pt-2">{{ category }}</h4>
                    <p>
                        <img class="card-img-top" src="{{ base_url }}{{ video_thumbnail_url }}torah_rich.webp">
                    </p>
                    <span class="col-md-6 justify-content-start"><small>{{ num_episodes }} episodes</small></span> 
                    <div class="col-md-6 btn-group dropdown float-end">
                        <button type="button" class="btn btn-sm rounded-pill btn-outline-primary dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa fa-upload"></i> &nbsp;Share
                        </button>
                        <ul class="dropdown-menu" style="">
                            <li>
                                <a class="dropdown-item" href="javascript:void(0);">
                                    <div class="a2a_kit a2a_kit_size_32 a2a_default_style">
                                <a class="a2a_button_facebook"></a>
                                <a class="a2a_button_x"></a>
                                <a class="a2a_button_whatsapp"></a>
                                <a class="a2a_button_snapchat"></a>
                                <a class="a2a_button_tumblr"></a>
                                </div>
                                </a>
                            </li>
                        </ul>
                    </div>
                    <p class="lead mt-4">
                        The Torah, or the first five books of the Bible, is one epic storyline that starts with creation and ends with God’s covenant with Israel.
                    </p>
                </aside>
            </div>
        </div>
        <div class="col-lg-9 align-items-center">
            {% for vid in episodes %}
            <div class="row mb-3 card border-radius-0 border-0 box-shadow-1">
                <div class="align-content-center">
                    <img class="float-start" style="height: 120px; margin: 20px;" src="{{ base_url }}{{ video_thumbnail_url }}{{ vid.thumbnail }}" alt="{{ vid.title_raw }}">
                    <h5 class="mt-3">{{ vid.title_raw }}</h5>
                    {{ vid.intro }}                
                    <a class="btn btn-primary btn-with-arrow mb-2 float-end" href="{{ base_url }}videos/watch/{{ vid.title|replace({' ': '-'}) }}">
                    Watch Now <span><i class="fas fa-chevron-right"></i></span>
                    </a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}